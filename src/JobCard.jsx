function JobCard({ job }) {
  const {
    jdUid: id,
    jdLink: jobLink,
    jdDetailsFromCompany: details,
    maxJdSalary: maxSalary,
    minJdSalary: minSalary,
    salaryCurrencyCode,
    location,
    minExp,
    maxExp,
    jobRole,
    comapnyName,
    logoUrl,
  } = job;

  return <div></div>;
}

export default JobCard;
